{
  "name": "pinge-docs",
  "private": true,
  "scripts": {
    "gen": "node scripts/gen-site-data.mjs",
    "predev": "node scripts/gen-site-data.mjs",
    "dev": "vitepress dev --host 0.0.0.0 --port 15173",
    "prebuild": "node scripts/gen-site-data.mjs",
    "build": "vitepress build",
    "preview": "vitepress preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
