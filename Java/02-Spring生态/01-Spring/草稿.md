
1、创建一个Spring 容器 `ApplicationContext`，接收一个配置对象，在配置对象里告诉Spring去扫描哪个路径下的类，Spring则会去扫描该路径下的所有类，然后看对应的类上是否有 `@Componnent`注解，有的话则创建为Bean

2、从Spring容器中就可以获取指定的Bean

>在早期，配置是通过XML来完成的，由于当时Java注解来没有出现，后来普遍使用配置类+注解的方式。