
保守：
1. 安全性和可控性优先于便利性
2. 显式优于隐式
# 安全性
## 跨平台安全性

Java程序的运行不依赖于特定的操作系统或硬件平台，而是通过Java虚拟机（JVM）实现跨平台运行。JVM作为中间层，可以屏蔽底层平台的差异性和潜在的安全漏洞，从而保护Java程序免受底层平台安全问题的影响。

## 面向对象安全性

Java的封装特性使得Java能够隐藏对象的内部状态，只通过公共的接口与外界交互，从而减少了外部对内部状态的直接访问，提高了数据的安全性。

## 严格的安全机制

1. 无指针运算：Java中不直接支持指针操作，而是通过引用（reference）来访问对象。这种机制避免了指针的野指针、越界等问题，提高了内存级的安全性。（有指针：c，go等）
2. 数组边界检查：Java在数组访问时会进行边界检查，防止数组越界访问，从而避免了C/C++中常见的缓存溢出等安全漏洞。
3. 强制类型转换：Java是强类型语言，在编译器期间需要保证类型一致，否则必须进行显式转换，如果转换类型不兼容，会抛出`ClassCastException`，这有助于在编译时或运行时发现潜在的错误。
4. 安全管理器（SecurityManager）：Java提供了一个安全管理器，用于限制Java程序对系统资源的访问权限，如文件访问、网络访问等。通过配置安全管理器，可以进一步增强Java程序的安全性。要启用安全管理器，你需要在程序启动时通过`System.setSecurityManager(SecurityManager s)`方法设置一个安全管理器实例。一旦设置了安全管理器，JVM在执行受保护的操作之前就会自动调用相应的方法进行权限检查。
⚠️ 注意：从 **Java 17 开始，SecurityManager 被标记为“弃用（deprecated）”**；从 **Java 18 起，其功能被进一步削弱**；**在未来的 Java 版本中将被彻底移除**。Oracle 官方认为现代应用更依赖于模块化、容器化和操作系统级别的安全机制，而非运行时权限检查。



# Builder模式


用于构建复杂对象，尤其是当一个类有很多可选参数时，甚至参数本身也是复杂的对象时，它的核心思想是：**将对象的构建过程与其表示分离，使得同样的构建过程可以创建不同的表示**。
分步骤创建复杂对象

```Bash
Product（产品）
    └── 复杂对象
    
Builder（建造者接口）
    ├── 定义创建产品各个部分的抽象方法
    └── 返回产品的抽象方法
    
ConcreteBuilder（具体建造者）
    ├── 实现Builder接口
    └── 负责具体构建过程
    
Director（指挥者）- 可选
    └── 控制构建过程
```

简单案例演示

```Java
// 产品类
public class Computer {
    private String CPU;
    private String RAM;
    private String storage;
    private String graphicsCard;
    
    // 私有构造函数
    private Computer(Builder builder) {
        this.CPU = builder.CPU;
        this.RAM = builder.RAM;
        this.storage = builder.storage;
        this.graphicsCard = builder.graphicsCard;
    }
    
    public static Builder builder() {  
		return new Builder();  
	}
    
    // 静态内部Builder类
    public static class Builder {
        // 必需参数
        private String CPU;
        private String RAM;
        
        // 可选参数 - 有默认值
        private String storage = "512GB SSD";
        private String graphicsCard = "Integrated";
        
        public Builder(String CPU, String RAM) {
            this.CPU = CPU;
            this.RAM = RAM;
        }
        
        public Builder storage(String storage) {
            this.storage = storage;
            return this;
        }
        
        public Builder graphicsCard(String graphicsCard) {
            this.graphicsCard = graphicsCard;
            return this;
        }
        
        public Computer build() {
            return new Computer(this);
        }
    }
    
    // Getters...
}
```

使用
```Java
// 链式调用，清晰易读
Computer gamingPC =  Computer.builder().storage("2TB NVMe SSD").graphicsCard("NVIDIA RTX 4080").build();
```